This is my rshell project

WHATIS:
rshell is a shell that goes on top of bash

WHATS SUPPORTED in
-version 10212014-hw01:
--echo
--cd
--pwd
--ls, with multiple flag support
-version 11112014-hw1:
--ls flags -a, -l, -R, and combinations

KNOWN BUGS in
-version from 10212014:
--cannot handle comment
--cannot handle connectors
--cannot access non-bin commands such as 'cd'
--cannot finger self
- version from 11112014
--flag -lR/Rl lists long format for current directory, simply for nested
--flag -aR/Ra has a "no such file or directory error" 

POSSIBLE BUGS:
-memory leaks
